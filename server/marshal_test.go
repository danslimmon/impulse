package server

import (
	/*
		"github.com/stretchr/testify/assert"
	*/
	"testing"
)

func TestUnmarshalSection(t *testing.T) {
	/*
		t.Parallel()
		assert := assert.New(t)
		var data []byte
		var rslt *TreeNode
		var err error

		data = []byte("check twitter\n\tboil water\nmake pasta\n")
		rslt, err = unmarshalSection(data)
		assert.Nil(err)
		assert.Equal(2, len(rslt.Children))
		assert.Equal("check twitter", rslt.Children[0].Referent.GetTitle())
		assert.Equal("make pasta", rslt.Children[1].Referent.GetTitle())
		assert.Equal(1, len(rslt.Children[1].Children))
		assert.Equal("boil water", rslt.Children[1].Children[0].Referent.GetTitle())
	*/
}
